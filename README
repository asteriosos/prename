Perl rename(prename) as a separate package
=================================

Removes confusion about the util-linux vs Perl versions of rename.

Installs into /usr/local/bin/prename, so you can use booth.
Systems rename and prename.

Installation
------------

sudo make install
